// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: 'AIzaSyDR3egL4BXjCfdufHcD4XdBGUiLj-NSO9c',
    authDomain: 'checklist-46db7.firebaseapp.com',
    databaseURL: 'https://checklist-46db7.firebaseio.com',
    projectId: 'checklist-46db7',
    storageBucket: 'checklist-46db7.appspot.com',
    messagingSenderId: '133266897694',
    appId: '1:133266897694:web:e926fb45198d7ca789e70f'
  }
};

  // Your web app's Firebase configuration

  // // Initialize Firebase
  // firebase.initializeApp(firebaseConfig);

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
